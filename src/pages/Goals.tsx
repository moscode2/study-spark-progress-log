import { Layout } from '@/components/Layout';
import { Dashboard } from '@/components/Dashboard';

const Goals = () => {
  return (
    <Layout>
      <Dashboard />
    </Layout>
  );
};

export default Goals;